apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-rearaoke-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: green
  template:
    metadata:
      labels:
        app: green
    spec:
      containers:
      - name: rearaoke
        image: nmarklund10/rearaoke:<TAG>
        ports:
        - containerPort: 3000
        imagePullPolicy: Always
      strategy:
        type: Recreate